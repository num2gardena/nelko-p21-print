/**
 * Data / merge / batch (Phase 6).
 *
 * CSV import (papaparse), binding of canvas objects to columns (replacing the
 * {Column} / qr_Column placeholder system), per-row preview, batch printing
 * with progress, and PNG export.
 */
export {};
