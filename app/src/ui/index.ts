/**
 * Device panel UI (Phase 7).
 *
 * Connect / auto-detect, readiness + RFID label info, battery, config, and the
 * timeout / beep / self-test controls — every current CLI flag surfaced as UI.
 */
export {};
