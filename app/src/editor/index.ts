/**
 * WYSIWYG label editor (Phase 5).
 *
 * fabric.js canvas sized to the active label's printable area, with tools for
 * text, QR codes, barcodes, images and shapes, plus a live 1-bit print-preview
 * pane driven by core/imaging. Templates serialise to/from fabric JSON, with
 * optional import of existing Inkscape SVG templates.
 */
export {};
